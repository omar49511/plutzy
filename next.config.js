/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "logos-world.net", "cdn.pixabay.com"], // Add any other domains you need here
  },
};

module.exports = nextConfig;
